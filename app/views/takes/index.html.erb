<p id="notice"><%= notice %></p>

<h1>Takes</h1>

<table class="table">
  <thead>
    <tr>
      <th>Course</th>
      <th>Studentt</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @takes.each do |take| %>
      <tr>
        <td><%= take.course %></td>
        <td><%= take.studentt %></td>
        <td><%= link_to 'Show', take %></td>
        <td><%= link_to 'Edit', edit_take_path(take) %></td>
        <td><%= link_to 'Destroy', take, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Take', new_take_path %>
